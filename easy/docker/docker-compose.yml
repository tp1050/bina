version: '3.8'
services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: beauty_user
      MONGO_INITDB_ROOT_PASSWORD: beauty123

volumes:
  mongodb_data: